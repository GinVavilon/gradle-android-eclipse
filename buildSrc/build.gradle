apply from: "${rootDir.parentFile}/properties.gradle"
repositories {
    jcenter()
    mavenCentral()
    google()
}

dependencies {
    implementation "com.android.tools.build:gradle:$androidVersionTools"
    implementation project(':plugin')
}
